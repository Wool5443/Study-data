\input{../labpreamb.tex}

\renewcommand{\labnumber}{2}
\renewcommand{\tasks}{(12, 3)}

\begin{document}

\maketitle
\thispagestyle{empty}
\newpage
\pagenumbering{arabic}
\tableofcontents

\newpage
\section{Задание}

\begin{enumerate}
    \item Даны целочисленная матрица X[1:n, 1:m] и целочисленный
        массив Z[1:k]. Обнулить элементы матрицы X, которых нет в
        массиве Z и запомнить обнуленные элементы.
    \item Дан массив целых положительных чисел. Сформировать новый
        массив, содержащий произведения цифр каждого элемента
        исходного массива.
\end{enumerate}

\newpage
\section{Постановка задачи}

\given{
    \item
        n, m, k --- цел. \\
        x[1:n, 1:m] --- цел. \\
        z[1:k] --- цел.
    \item
        n - цел.
        a[1:n] --- цел.
}
\result{
    \item
        t --- цел., количество обнулённых элементов \\
        e[1:t] --- обнулённые элементы или сообщение <<Ноль
        обнулений>>
    \item
        p[1:n] --- цел.
}
\prerequisites{
    \item
        $ 1 \leq n, m, k \leq LMAX$ \\
    \item
        $ 1 \leq n \leq LMAX$
}
\binding{
    \item
        $\forall i \in [1, k]: \exists (i, j) \in \{n \times m\}:
        x[i, j] = z[i] \implies x'[i, j] = 0, \exists k \in [1, t]:
        e[t] = z[i]$, где $x'$ --- массив после преобразования
    \item
        $\forall i \in [1, n] \implies p[i] = productdigits(a[i])$
}

\newpage
\section{Метод решения задачи}

\mathsolution{
    \mathsolutiontask{
        t = 0 \\
        \mathsolutionloop{
            \text{для } i = \overline{1, n} \\
            \mathsolutionloop{
                \text{для } j = \overline{1, m} \\
                found = 1 \\
                \mathsolutionloop{
                    \text{пока } found \leq k \text{ и } x[i, j]
                    \neq z[found] \\
                    found = found + 1
                } \\
                x[i, j] = 0 \text{, если } found \leq k \\
                t = t + 1 \text{, если } found \leq k \\
                e[t] = z[found] \text{, если } found \leq k
            }
        }
    }
    \mathsolutiontask{
        \mathsolutionloop{
            \text{для } i = \overline{1, n} \\
            t_{val} = a[i] \\
            t_{prod} = 1 \text{, если } t_{val} > 0 \\
            t_{prod} = 0 \text{, в противном случае} \\
            \mathsolutionloop{
                \text{пока } t_{val} > 0 \\
                t_{prod} = t_{prod} \cdot (t_{val} \bmod 10) \\
                t_{val} = t_{val} \bdiv 10 \\
            } \\
            p[i] = t_{prod}
        }
    }
}

\newpage
\section{Внешняя спецификация}

\specframe{
    Лабораторная работа №\labnumber
}

\specframe{
    Задание №1
}

\specframeloop{
    Введите $n$, $m$, $k$ от $1$ до $LMAX$: \\
    $<n>$, $<m>$, $<k>$
}
До $1 \leq n \leq LMAX$ и $1 \leq m \leq LMAX$ и $1 \leq k \leq LMAX$

\specframe{
    Введите матрицу $x$: \\
    $\ll x[1, 1] \gg$, $\ll x[1, 2] \gg$, ..., $\ll x[2, 1] \gg$, $\ll x[2, 2] \gg$, ..., $\ll x[n, m] \gg$,
}

\specframe{
    Введите массив $z$: \\
    $\ll z[1] \gg$, $\ll z[2]\gg$, ..., $\ll z[k] \gg$
}

При t = 0
\specframeif{
    Ноль обнулений
}
Иначе
\specframeif{
    Обнулено $<t>$ элементов: \\
    $\ll e[1] \gg$, $\ll e[2] \gg$, ..., $\ll e[t] \gg$ \\
    Матрица имеет следующий вид: \\
    $<x[1, 1]>$, $<x[1, 2]>$, ..., $<x[2, 1]>$, $<x[2, 2]>$, ..., $<x[n, m]>$,

}

\specframe{
    Задание №2
}

\specframeloop{
    Введите $n$ от $1$ до $LMAX$: \\
    $<n>$
}
До $1 \leq n \leq LMAX$

\specframe {
    Введите $a[1:n]$ \\
    $\ll a[1] \gg$, $\ll a[2]\gg$, ..., $\ll a[n] \gg$
}

\specframe{
    Массив произведений цифр: \\
    $\ll p[1] \gg$, $\ll p[2] \gg$, ..., $\ll p[n] \gg$
}

\newpage
\section{Описание алгоритма на псевдокоде}

% +algquote[]
% +algindex[]
% +algline[]
% +algplus
% +algleq
% +algneq
% +algfor[][]
% +algwhile[]
% +algforwhileend
% +alguntil
% +alguntilend[]
% +algif[]
% +algelse
% +algendif

\subsection{Программа №1}

\begin{Verbatim}[commandchars=\\\{\}]
алг \algquote{Лабораторная работа №\labnumber Задание №1}
    вывод(\algquote{Лабораторная работа №\labnumber})
    вывод(\algquote{Задание №1})

    вывод(\algquote{Введите n, m, k от 1 до LMAX:})

    \algline{цикл}
        ввод(n, m, k)
    \algline{до} 1 \algleq n \algleq LMAX \algline{и} 1 \algleq n \algleq LMAX \algline{и} 1 \algleq n \algleq LMAX

    вывод(\algquote{Введите матрицу x[1:n, 1:m]})
    ввод(x[1:n, 1:m])

    вывод(\algquote{Введите массив z[1:k]})
    ввод(z[1:k])

    t := 0

    \algfor{i := 1}{n}
        \algfor{j := 1}{m}
            found := 1
            \algwhile{found \algleq k \algline{и} x[i, j] \algneq z[found]}
                found := found \ 1
            \algforwhileend
            \algif{found \algleq k}
                x[i, j] := 0
                t := t + 1
                e[t] = z[found]
            \algendif
        \algforwhileend
    \algforwhileend

    \algif{t = 0}
        вывод(\algquote{Ноль обнулений})
    \algelse
        вывод(\algquote{Обнулено}, t, \algquote{элементов:})
        вывод(e[1:t])
        Матрица имеет следующий вид:
        вывод(x[1:n, 1:m])
    \algendif
кон
\end{Verbatim}

\subsection{Программа №2}

\begin{Verbatim}[commandchars=\\\{\}]
алг \algquote{Лабораторная работа №\labnumber Задание №2}
    вывод(\algquote{Лабораторная работа №\labnumber})
    вывод(\algquote{Задание №2})

    вывод(\algquote{Введите n от 1 до LMAX:})

    \algline{цикл}
        ввод(n)
    \algline{до} 1 \algleq n \algleq LMAX

    вывод(\algquote{Введите массив a[1:n]})
    ввод(a[1:n])

    \algfor{i := 1}{n}
        t_val := a[i]
        \algif{t_val > 0}
            t_prod := 1
        \algelse
            t_prod := 0
        \algendif
        \algwhile{t_val > 0}
            t_prod := t_prod \algcdot (t_val mod 10)
            t_val := t_val div 10
        \algforwhileend
        p[i] := t_prod
    \algforwhileend
    вывод(\algquote{Массив произведений цифр:})
    вывод(p[1:n])
кон
\end{Verbatim}

\newpage
\section{Листинг программы}

\subsection{Программа №1}
\lstinputlisting[language=C]{task1.c}

\subsection{Программа №2}
\lstinputlisting[language=C]{task2.c}

\newpage
\section{Распечатка тестов к программе и результатов}

\subsection{Программа №1}

\begin{center}
    \begin{tabular}{|c|l|l|}
        \hline
        № & Искодные данные & Результат \\
        \hline
        1 &
        $
        n = 3, \  m = 3, \  k = 2, \
        x =
            \begin{Vmatrix}
                1 & 2 & 3 \\
                4 & 5 & 6 \\
                7 & 8 & 9
            \end{Vmatrix}
        , \ z = \{4, \ 5\}
        $ &
        \begin{tabular}{l}
            Обнулено 2 элементов: \\
            4 5 \\
            Матрица имеет следующий вид: \\
            $
                \begin{Vmatrix}
                    1 & 2 & 3 \\
                    0 & 0 & 6 \\
                    7 & 8 & 9
                \end{Vmatrix}
            $
        \end{tabular} \\
        \hline
        2 &
        $
        n = 3, \  m = 3, \  k = 2, \
        x =
            \begin{Vmatrix}
                1 & 2 & 3 \\
                4 & 5 & 6 \\
                7 & 8 & 9
            \end{Vmatrix}
        , \ z = \{10, \ 11\}
        $ &
        \begin{tabular}{l}
            Ноль обнулений
        \end{tabular} \\
        \hline
    \end{tabular}
\end{center}

\subsection{Программа №2}

\begin{center}
    \begin{tabular}{|c|l|l|}
        \hline
        № & Искодные данные & Результат \\
        \hline
        1 & $n = 5, \ a = \{1, \  60, \  56, \  3, \  11111\}$ &
        \begin{tabular}{l}
            $a = \{1, \  0, \  30, \  3, \  1\}$
        \end{tabular} \\
        \hline
        1 & $n = 5, \ a = \{1, \  56, \  0, \  3, \  11111\}$ &
        \begin{tabular}{l}
            $a = \{1, \  30, \  0, \  3, \  1\}$
        \end{tabular} \\
        \hline
    \end{tabular}
\end{center}

\end{document}
