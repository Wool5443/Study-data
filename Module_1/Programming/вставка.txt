алг "Вставка"
нач
    ввод(n, A[1:n], B[1:m])

    nmin := 1
    цикл от i := 2 до n
        если A[i] < A[nmin] то
            nmin := i
        всё
    кц

    если nmin != n то
        цикл от i := n до nmin + 1 шаг -1
            A[i + m] := A[i]
        кц
    всё

    цикл от i := 1 до m
        A[nmin + i] := B[i]
    кц

    n := n + m

    вывод(A[1:n])
кон
