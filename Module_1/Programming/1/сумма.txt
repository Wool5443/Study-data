алг "Сумма"
нач
    ввод(A[1:n])

    n1 := 0
    np := 0

    цикл от i := 1 до n
        если A[i] < 0 то
            если n1 = 0 то
                n1 := i
            всё
            np := i
        всё
    кц

    если n1 = 0 то
        вывод("нет отрицательных элементов")
    иначе если n1 = np то
        вывод("один отрицательный элемент ", a[n1])
    иначе
        s := 0
        цикл от i = n1 до np
            s := s + A[i]
        кц
        вывод(s)
    всё

кон
