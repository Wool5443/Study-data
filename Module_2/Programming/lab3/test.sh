#!/usr/bin/env bash

for file in $1.*.txt; do
    echo $file
    cat $file
    echo "------------------------------------------------------------------"
    cat $file | ./task$1
    echo "------------------------------------------------------------------"
done
