#!/usr/bin/env bash

for file in 1.*.txt; do
    echo $file
    cat $file
    echo "------------------------------------------------------------------"
    cat $file | ./main
    echo "------------------------------------------------------------------"
done
